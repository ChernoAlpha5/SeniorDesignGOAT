package com.dfrobot.angelo.blunobasicdemo.graphData;

import android.support.v7.app.AppCompatActivity;
import com.dfrobot.angelo.blunobasicdemo.R;
/**
 * Created by Andrew on 2/18/2017.
 */

public class GraphHelper  extends AppCompatActivity {
   /* HydrationFragment hFrag;
    //ViewPagerAdapter pagerAdapter;

    public GraphHelper(){
        //pagerAdapter = new GraphActivity().getPagerAdapter();
        hFrag = (HydrationFragment)findViewById(R.id.hydrateChart);
    }

    public void updateGraph(String theString){
        final String input = theString;
        runOnUiThread(new Runnable() {
            @Override
            public void run() {
                //hydrateFrag = (HydrationFragment) getSupportFragmentManager().findFragmentById(R.id.hydration_fragment);
                if (pagerAdapter != null && hFrag != null){
                    hFrag = (HydrationFragment) pagerAdapter.getFragment(0);  //is 0 the position in the hashmap??
                    hFrag.hydrateDispMsg2(input);
                }

            }
        });
    }*/


}
